#ifndef GAMMA_INCLUDED
#define GAMMA_INCLUDED

double log_gamma (double z);
double log_beta_dist (double x, double noWeight, double yesWeight);
double log_gamma_dist (double x, double countParam, double timeParam);

#endif /* GAMMA_INCLUDED */
